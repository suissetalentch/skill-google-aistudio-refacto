# Gemini API key — use VITE_ prefix for client-side access
# WARNING: Client-side API keys are visible in browser DevTools.
# For production, proxy AI calls through a backend API.
VITE_GEMINI_API_KEY=your_api_key_here
